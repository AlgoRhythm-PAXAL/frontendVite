const LoginInput = (props) => {
  return (
    <input
      {...props}
      className="w-full pl-10 pr-3 py-2 bg-opacity-50 rounded-lg border border-Primary   placeholder-gray-400 "
    />
  );
};

export default LoginInput;
